require("bufferline").setup{
    options = {
        diagnostics = 'nvim_lsp',
        sort_by = 'id',
        show_close_icon = false,
        max_name_length = 25,
        tab_size = 25,
        show_buffer_close_icons = false,
        numbers = 'ordinal',
    }
}
